To run, you will need to use the terminal/command line
unzip the file, you will have the file Breakout.py
Open the terminal/command line and use python3 Breakout.py to run it

You may need to install tkinter, pysimplegui to do so:
pip3 install pysimplegui 
and 
pip3 install tk
